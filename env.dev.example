# 本地开发环境变量配置（示例文件）
# 使用方法：复制此文件为 backend/.env

# ==================== 服务器配置 ====================
PORT=3000
NODE_ENV=development

# API 基础 URL（本地开发可留空，自动使用当前请求域名）
API_BASE_URL=http://localhost:3000

# ==================== 数据库配置 ====================
# 连接本地 Docker MySQL（端口 3307）
DB_HOST=localhost
DB_PORT=3307
DB_NAME=accounting_miniapp
DB_USER=accounting_dev
DB_PASSWORD=dev123456

# ==================== JWT 配置 ====================
# 开发环境使用简单密钥（生产环境必须更换）
JWT_SECRET=dev_jwt_secret_key_for_local_development_only

# ==================== 微信小程序配置 ====================
# 请填写你的微信小程序 AppID 和 AppSecret
WX_APPID=your_wechat_appid
WX_SECRET=your_wechat_appsecret

# ==================== 说明 ====================
# 1. 使用前请先启动 Docker MySQL：
#    docker-compose -f docker-compose.dev.yml up -d
#
# 2. 本配置文件已设置为连接本地 Docker MySQL（端口 3307）
#
# 3. 微信小程序配置需要到微信小程序后台获取：
#    https://mp.weixin.qq.com/
#
# 4. 生产环境部署时，请使用独立的 .env 文件并修改所有密钥

